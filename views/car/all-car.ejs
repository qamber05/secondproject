<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>All cars page</h1>
    <% allCars.forEach(element => { %>
        <h2><%= element.carName %></h2>
        <h3><%= element.carModel %></h3>
        <h3><%= element.carHorsepower %></h3>
        <a href="/cars/<%= element._id %>/update">Update me</a>
        <a href="/cars/<%= element._id %>/delete">Delete me</a>
        
        <% }); %>
</body>
</html>